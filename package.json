{
  "name": "nestjs-caching-module",
  "version": "1.1.0",
  "description": "A configurable caching module for NestJS supporting in-memory and Redis stores",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["nestjs", "cache", "redis", "lru-cache"],
  "author": "UnCompa",
  "license": "ISC",
  "dependencies": {
    "@nestjs/common": "^11.1.3",
    "ioredis": "^5.6.1",
    "lru-cache": "^11.1.0"
  },
  "devDependencies": {
    "typescript": "^4.5.0"
  }
}
